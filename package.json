{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "start": "npm start --prefix packages/bank-app",
    "postinstall": "lerna run bootstrap",
    "prepublishOnly": "npm build && npm test",
    "deploy": "lerna version && lerna publish from-package"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^12.11.1",
    "jest": "^27.4.7",
    "jest-preset-angular": "^11.1.0",
    "lerna": "^4.0.0",
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.2",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
